// TypeScript bitches about these imports, but it will work fine
// Just make sure you import declare the same var name as the file
import AlbumCard from './album/AlbumCard.vue'
import PhotoCard from './photo/PhotoCard.vue'
import PhotoList from './photo/PhotoList.vue'
import UserPanel from './user/UserPanel.vue'

export { AlbumCard, PhotoCard, PhotoList, UserPanel }
