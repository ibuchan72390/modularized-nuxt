import Vue from 'vue'

/*
 * Sample mixing we can use to demonstrate that this gets applied to all components
 * This will log every component's unique Identifier upon mounting it to the page
 * Simple and stupid, yet proves a basic point
 * https://alligator.io/vuejs/creating-custom-plugins/#your-first-plugin
 */
// Vue.mixin({
//   mounted() {
//     console.log('Hello from global component plugin!  This is from component: ' + this._uid)
//   }
// })
